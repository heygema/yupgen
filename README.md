# YupTypeGenServer

What to do eh?

```bash
yarn install
yarn start
```

Hit request at http://localhost:8091 with:
```javascript
method: POST;
body: {
  file: string;
  data: any;
  typegen: true;
}
```
